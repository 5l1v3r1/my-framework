[description]
    access_token = facebook access token, file or string
    threads = number of thread
    file = file to process
    status = status boolean, true or false
    url = specify link
    total = total of result (int)
